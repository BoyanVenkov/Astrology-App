# Loaded only by `vite build --mode review` (npm run build:review).
# Unlocks every Pro feature so the whole app can be reviewed on a device
# without a purchase. Never used by a normal `npm run build`.
VITE_REVIEW_UNLOCK=1
